services:
  - type: web
    name: game-portal
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    disk:
      name: game-data
      mountPath: /data
      sizeGB: 1
